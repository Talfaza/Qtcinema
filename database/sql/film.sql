CREATE TABLE film (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    titre TEXT,
    realisateur TEXT,
    duree INTEGER,
    genre TEXT,
    dateSortie TEXT,
    description TEXT,
    quantite INTEGER unsigned
);  